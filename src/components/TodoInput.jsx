import React from "react";

const TodoInput = () => {
  return (
    <header>
      <input type="text" placeholder="Enter todo..." />
      <button>Add</button>
    </header>
  );
};

export default TodoInput;
